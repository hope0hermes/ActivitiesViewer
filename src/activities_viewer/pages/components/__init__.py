"""
Reusable Streamlit components for pages.

This module contains extracted UI rendering functions that were previously
embedded in page main() functions. Each component is a pure function that
handles a specific UI section.
"""
